# CEM DOM Projection Schema

This package defines the semantic CEM DOM projection layer:

- schema URL: `https://cem.dev/ns/projection/dom/1`
- primary content type: `application/vnd.cem.dom+cem-bin`
- debug/interchange view: `application/vnd.cem.dom+json`

The DOM projection is binary-first. JSON output is a renderer over the same
semantic layer, including the legacy
`https://cem.dev/ns/projection/dom-json/1` export.
