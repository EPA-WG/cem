# cem-components

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build cem-components` to build the library.

## Running unit tests

Run `nx test cem-components` to execute the unit tests via [Vitest](https://vitest.dev/).
